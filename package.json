{
  "name": "hardhatsimplesmartcontract",
  "version": "1.0.0",
  "description": "simple smart contract",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npx hardhat run scripts/deploy.ts",
    "deployprivate": "npx hardhat run scripts/deploy.ts --network test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.13.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
